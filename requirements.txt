# Analytics Accelerator – portable requirements
# Core app + data handling
streamlit>=1.34
pandas>=2.2
numpy>=1.26

# Visualization
altair>=5.3

# Stats / clustering (used for outliers, distances)
scipy>=1.11

# File formats & large JSON
pyarrow>=14
openpyxl>=3.1   # .xlsx reader/writer
xlrd>=2.0       # .xls support
ijson>=3.2      # streaming JSON

# Connectors (DataOS/Trino tab)
trino>=0.328.0

# Optional but recommended features
reportlab>=4.0  # PDF export on Final Summary page
wordcloud>=1.9  # Word cloud in Text Preprocessing (falls back if missing)
nltk>=3.8       # Lightweight stemming if available (falls back if missing)

# Notes:
# - This file avoids OS-specific packages and heavy unused deps to stay portable.
# - If you don't need PDF export or word clouds, you can comment out 'reportlab'/'wordcloud'.
# - Pandas will use 'openpyxl' for .xlsx and 'xlrd' for .xls automatically.
